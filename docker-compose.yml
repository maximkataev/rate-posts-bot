services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telegram-post-evaluator
    env_file:
      - .env
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
